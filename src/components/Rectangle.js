import React from 'react'
import {WithRandomBackground} from './withRandomBackground'

const Rectangle = (props) => {
  return(
    <div className='rectangle'>  
    </div>
  );
}
export default WithRandomBackground(Rectangle);
